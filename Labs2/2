Да се внесат позитивни цели броеви и за секој влез да се испечати која е најголемата цифра во бројот.
Доколку се внесе нешто што не е број, да се испечати најголемата цифра

#include <iostream>
using namespace std;
int main()
{
    int x;
    int Max, suma=0, brojac=0;
    while(cin>>x){
        int temp=x;
        Max=0;
            while(temp>0){
                int last=temp%10;
                if(last>Max){
                    Max=last;
                }
                temp/=10;
            }
            cout<<Max<<endl;
            suma+=Max;
            brojac++;
    }
    cout<<suma/brojac;
    return 0;
}
