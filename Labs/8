Студент треба да се запише на ФИНКИ. Доколку сака да се запише треба да пристигне во студентската служба пред 9 
часот и да чека ред да биде услужен. Студентската служба отвора во 
  9:00 - 11:00 работи
  11:00 - 11:30 пауза 
  11:30 - 12:30 работи
  12:30 - 13:00 пауза
  13:00 - 15:00 работи и затвора 
Ваша задача е, доколку знаете кој по ред е студентот во редицата за чекање, да определите и испечатите
времето во минути и часови кое тој го чека за да биде услужен. 

#include <iostream>
using namespace std;

int main() {
    int red;
    cin >> red;
    int vreme;
    if (red <= 24) {
        vreme = (red * 5) - 5;
    } else if (red > 24 && red <= 36) {
        vreme = (red * 5) + 30 - 5;
    } else if (red > 36 && red <= 70) {
        vreme = (red * 5) + 60 - 5;
    }
    int casovi = vreme / 60;
    int minuti = vreme % 60;
    cout << casovi << " casovi i " << minuti << " minuti." << endl;
    return 0;
    }

